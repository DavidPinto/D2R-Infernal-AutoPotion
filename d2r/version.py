"""Single source of truth for the tool version.

Bumped per release.  The UI title, `--version` output, and the documentation all
reference this so version and code can never drift apart.
"""

__version__ = "1.9.2-beta"
